import { Placement } from './placements';
export declare class TwClassHelpers {
    static buildIconButtonClass(placement: Placement, base: string): string;
    static buildIconClass(placement: Placement, size: string): string;
}
